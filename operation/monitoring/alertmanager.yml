global:
  slack_api_url_file: "/etc/alertmanager/slack_url"

route:
  receiver: "slack-alerts"
  repeat_interval: "4h"
  group_by: [alertname]
receivers:
  - name: "slack-alerts"
    slack_configs:
      - channel: "#all-app-alerts"
        send_resolved: true
